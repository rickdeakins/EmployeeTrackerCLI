const connection = require('./server')

function addEmployee() {
    //user should be prompted for all fields of data
}

function addDepartment() {}

function addTitle() {}

function addSalary() {}

function updEmployeeName() {}

function updDepartment() {}

function updTitle() {}

function removeRecord() {
    // Prompt user for employee ID then SQL DELETE command to remove the employee from the database 
}

function viewEmployees() {
    // Prompt user for employee ID then SQL DELETE command to remove the employee from the database 
}

module.exports = { addEmployee, addDepartment, addTitle, addSalary, updEmployeeName, updDepartment, updTitle, removeRecord, viewEmployees };